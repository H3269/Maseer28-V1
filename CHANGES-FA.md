# تغییرات نسخه Reader UX

## حذف مدیریت کتاب

- مسیر و صفحه مدیریت کتابخانه حذف شد.
- کد Import فایل‌های PDF/DOCX مدیریتی و وابستگی‌های `mammoth` و `pdfjs-dist` حذف شد.
- کتابخانه اکنون فقط رابط کاربری مطالعه است.

## اصلاح تجربه کتاب‌خوان

- فصل‌ها و بخش‌های اصلی روی صفحه عنوان مستقل باز می‌شوند.
- تیترهای داخلی با سلسله‌مراتب بصری مشخص نمایش داده می‌شوند.
- برای تیترها منطق `keep with next` اعمال شده تا تیتر در انتهای صفحه تنها نماند.
- در جلد سوم، بخش عملی بعد از فصل ۱۴ و سپس ضمائم در محل واقعی عنوان خود جدا می‌شوند؛ حتی وقتی عنوان وسط یک صفحه مرجع Word قرار گرفته باشد.
- «ضمیمه اول»، «ضمیمه دوم» و «ضمیمه سوم» جلد سوم به فهرست کتاب‌خوان اضافه شده‌اند.
- تقدیم‌نامه، سپاسگزاری، یادداشت مجموعه و پیشگفتارها در جلد سوم از ابتدای صفحه تازه شروع می‌شوند.
- جدول‌ها دیگر به خطوط متن شکسته نمی‌شوند؛ به صورت جدول واقعی با اسکرول افقی نمایش داده می‌شوند.
- عنوان جدول، در مواردی که از متن منبع قابل تشخیص باشد، همراه خود جدول نگه داشته می‌شود.
- دکمه‌های صفحه قبل/بعد در مرز کتاب غیرفعال می‌شوند و پنجره مزاحم «پایان کتاب» حذف شده است.

## رفتار اجرای مجدد

- پس از تکمیل onboarding، اجرای تازه برنامه همیشه از صفحه خانه/داشبورد شروع می‌شود.
- محل مطالعه هر کتاب مستقل از صفحه فعلی برنامه ذخیره می‌شود و هنگام باز کردن دوباره همان کتاب بازیابی می‌شود.
- جریان اولین اجرا و کپی‌رایت/ثبت پرونده دست‌نخورده باقی مانده است.

## V81 — صفحه‌آرایی و سلسله‌مراتب فارسی بدون تغییر محتوا

- عنوان صفحه‌ی آغاز هر فصل بزرگ‌تر و واضح‌تر شد؛ فصل همچنان از صفحه‌ی مستقل آغاز می‌شود.
- «فهرست مطالب» دیگر همه‌ی عنوان‌ها را با یک اندازه نشان نمی‌دهد: عنوان فصل از زیرعنوان‌ها بزرگ‌تر است، عنوان «بخش» مرتبه‌ی مستقل دارد و زیرعنوان‌ها سبک‌تر نمایش داده می‌شوند.
- در فهرست مطالب، عنوان هر فصل همراه زیرعنوان‌های همان فصل تا جای ممکن در یک صفحه نگه داشته می‌شود و بین دو صفحه نصف نمی‌شود؛ فقط وقتی مجموعه‌ی یک فصل واقعاً از ظرفیت یک صفحه بیشتر باشد شکستن آن مجاز است.
- تیترهای اصلی داخل متن حداقل با سه سطر بعدی نگه داشته می‌شوند تا تیتر در انتهای صفحه تنها نماند.
- تشخیص تیترهای جلدهای ۱ و ۲ محافظه‌کارانه‌تر شد تا جمله‌های معمولی اشتباهاً تیتر نشوند.
- محدوده‌ی واقعی فهرست مطالب بهتر تشخیص داده می‌شود تا متن مقدمه/دفتر کار بعد از فهرست به شکل «آیتم فهرست» نمایش داده نشود.
- متن نمایشی دیگر بازنویسی و ویرایش واژگانی/نشانه‌گذاری نمی‌شود؛ فقط نویسه‌های کنترلی و آرتیفکت فنی استخراج حذف و فاصله‌های ضروری برای رندر نرمال می‌شوند. ترتیب و واژگان منبع دست‌نخورده می‌ماند.
- منطق جداول، فصل‌های مستقل، بازیابی موقعیت مطالعه و سایر اصلاحات V80 حفظ شده است.

## V82 — شکست صفحه برای تمرین‌های عملی

- بدون تغییر هیچ واژه یا جمله‌ای از محتوای کتاب، «تمرین عملی شماره …» به‌عنوان یک واحد مستقل صفحه‌آرایی می‌شود و همیشه از صفحه تازه آغاز می‌شود.
- اگر بلافاصله پس از عنوان تمرین، یک زیرعنوان کوتاه وجود داشته باشد (مثل «شناسایی چرخه ناخودآگاه»)، همان متن بدون بازنویسی به‌صورت زیرعنوان تمرین نمایش داده می‌شود.
- عنوان تمرین، زیرعنوان و چند خط آغازین متن با هم نگه داشته می‌شوند تا تیتر در انتهای صفحه یا جدا از محتوای مربوط به خودش رها نشود.
- همین قاعده برای «تمرین جامع فصل»، «کاربرگ فصل …» و «دفتر کار عملی» نیز اعمال شده است؛ فقط ساختار صفحه‌بندی تغییر می‌کند و محتوای کتاب دست‌نخورده می‌ماند.

## V83 — منوی پایین همیشه در دسترس پس از ورود اولیه

- منوی اصلی پایین برنامه پس از تکمیل ورود اولیه/ثبت پرونده و ارزیابی پایه، در تمام صفحات برنامه ثابت و در دسترس می‌ماند.
- این منو در صفحات روزانه، نتیجه روز، ارزیابی نهایی، گزارش، آرشیو، کتابخانه، پشتیبانی، ویرایش پروفایل و خود کتاب‌خوان نیز نمایش داده می‌شود.
- در اولین ورود کاربر، تا زمانی که مراحل اولیه (ثبت پرونده، تنظیم مسیر و ارزیابی اولیه) کامل نشده‌اند، منوی پایین نمایش داده نمی‌شود و منطق onboarding فعلی حفظ می‌شود.
- داخل کتاب‌خوان، کنترل‌های ورق‌زدن بالاتر از منوی اصلی قرار می‌گیرند تا دو نوار روی یکدیگر نیفتند.
- وضعیت انتخاب‌شده منوی پایین برای صفحات وابسته نیز منطقی شده است: کتاب‌خوان زیر «مطالعه» و صفحات مسیر روزانه/گزارش زیر «خانه» نمایش داده می‌شوند.


## V84 — صفحه آغازین تصویری جلد سوم

- تصویر ارسالی نویسنده به ابتدای جلد سوم در کتاب‌خوان افزوده شد.
- ترتیب نمایش جلد سوم اکنون: **جلد اصلی → صفحه آغازین تصویری → متن کتاب** است.
- تصویر به‌صورت `contain` و بدون برش نمایش داده می‌شود تا نوشته‌ها، لوگو و QR/اطلاعات پایین صفحه حذف نشوند.
- این تغییر فقط نمایشی است و هیچ متن، جمله، ترتیب یا محتوای کتاب تغییر نکرده است.

## V85 — ساختار دقیق جلد سوم، زیرعنوان‌ها و بازیابی جدول‌ها

- فهرست جلد سوم بر اساس ساختار خود فایل منبع اصلاح شد: بعد از فصل چهاردهم، «بخش عملی کتاب / مسیر ۲۸ روزه» به عنوان بخش مستقل می‌آید و پس از آن «ضمائم» مستقل است؛ هیچ‌کدام ادامه فصل چهاردهم محسوب نمی‌شوند.
- عنوان‌های «بخش عملی کتاب» و «ضمایم/ضمائم کتاب» حتی اگر در میانه صفحه مرجع Word باشند، از همان محل واقعی به صفحه عنوان مستقل تبدیل می‌شوند.
- در فهرست مطالب، «بخش»، «فصل» و «زیرعنوان» سه مرتبه بصری متفاوت دارند و زیرعنوان‌های هر فصل با همان فصل گروه‌بندی می‌شوند.
- تیترهای داخلی هر چهار جلد با سلسله‌مراتب درشت‌تر و بولد نسبت به متن نمایش داده می‌شوند؛ برای جلدهای ۱ و ۲ که قالب‌بندی Word تا حد زیادی تخت شده، عنوان‌ها از فهرست و الگوی ساختاری بازیابی می‌شوند. در جلدهای ۳ و تکمیلی، ساختار بلوک‌های منبع و قواعد اختصاصی اولویت دارند.
- تیترهای وارداتی که در واقع ادامه یک جمله بوده‌اند، دیگر صرفاً به دلیل خطای قالب‌بندی Word به شکل سرتیتر نمایش داده نمی‌شوند.
- ۹ جدول جلد سوم و ۱۴ جدول جلد تکمیلی در داده کتاب موجود و فعال‌اند و به صورت جدول واقعی و قابل پیمایش افقی نمایش داده می‌شوند.
- مشکل اصلی دیده نشدن اصلاحات/جدول‌ها برای کاربران قبلی برطرف شد: Seed کتابخانه اکنون «revision-aware» است و با انتشار نسخه جدید محتوای کتاب‌ها را از JSON جدید دوباره وارد می‌کند؛ بنابراین IndexedDB قدیمی دیگر کاربر را روی نسخه قبلی نگه نمی‌دارد.
- این نسخه هیچ واژه، جمله یا ترتیب محتوایی کتاب‌ها را بازنویسی نمی‌کند؛ تغییرات فقط در تشخیص ساختار، شکست صفحه و تایپوگرافی کتاب‌خوان است.

## V86 — حذف منوی اصلی پایین از داخل کتاب‌خوان

- منوی چهارگزینه‌ای اصلی برنامه (خانه، پروفایل، مطالعه، ارتباط) داخل صفحه مطالعه کتاب نمایش داده نمی‌شود.
- کتاب‌خوان فقط نوار و کنترل‌های اختصاصی خودش برای ورق‌زدن، پیشرفت، فهرست و تنظیمات مطالعه را نگه می‌دارد.
- منوی اصلی پایین در سایر بخش‌های برنامه، پس از تکمیل ورود اولیه، مطابق منطق قبلی همچنان ثابت و در دسترس است.
- فاصله اضافی‌ای که برای قرار گرفتن کنترل‌های کتاب‌خوان بالای منوی اصلی ایجاد شده بود حذف شد تا فضای مطالعه دوباره کامل در اختیار صفحه کتاب باشد.
